from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='simple project where we do a EDA + clustering analysis on classification data',
    author='Yacine',
    license='MIT',
)
